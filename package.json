{
  "name": "gamekit",
  "version": "1.4.0",
  "private": true,
  "description": "Shared components for Robert's family browser games (no build step; plain scripts on a GK global).",
  "scripts": {
    "test": "node --test"
  }
}
